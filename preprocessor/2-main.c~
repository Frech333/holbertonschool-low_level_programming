#include <stdlib.h>

#define UNUSED(x) (void)(x)

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    UNUSED(argv);

    _putchar(__FILE__);
    _putchar('\n');

    exit(EXIT_SUCCESS);
}
